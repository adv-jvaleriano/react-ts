import { CSSProperties } from "react";

export const STYLES = {
  MENU: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-evenly",
    alignItems: "center",
    padding: "10px 0px",
    textTransform: "uppercase",
  } as CSSProperties,
  BACK_BUTTON: {
    fontSize: "30px",
    cursor: "pointer",
  } as CSSProperties,
};
